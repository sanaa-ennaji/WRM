spring:
  datasource:
    url: jdbc:mysql://mysql:3306/wrm
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
  server:
    port: 8081
